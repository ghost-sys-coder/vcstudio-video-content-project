# Version-two build tracker

Last updated: 2026-09-10.

Product implementation: **0 of 20 slices verified**, with V2-01 through V2-07 implemented and awaiting browser or live acceptance, and V2-08 partially delivered. V2-01 implements local revisions, compatible media reuse, edit estimates and Short anchors. V2-02 implements autosave/recovery and atomic script approval. V2-03 implements verified script-to-scene fidelity with paid-failure usage recording and a review comparison; authenticated validation remains outstanding for all three. V2-00 has technical fixtures and the Money Made Clear baseline, with a measured creator walkthrough still pending. Five version-two migrations are generated/applied to the configured database; V2-03 required none. V2-01 through V2-07 are pushed to origin/master. The Trigger.dev worker was deployed as 20260910.1 for the V2-03 fidelity gate and the pasted-script fix; no other version-two web/worker deployment has occurred. V2-08's first increment adds `release_packages` and `release_package_revisions`, applied to the configured database as `20260910164608_release_packages` and covered by eight PostgreSQL integration tests that pass live.

## Status rules

| Status        | Meaning                                                                              |
| ------------- | ------------------------------------------------------------------------------------ |
| Not started   | No implementation work or acceptance evidence yet                                    |
| In progress   | Active implementation; record owner and next action                                  |
| Blocked       | Record the concrete blocker, dependent work, and resolution needed                   |
| In validation | Code exists, but acceptance, deployment, or live verification remains                |
| Verified      | All slice criteria and applicable checks are evidenced; deployment state is explicit |
| Deferred      | Explicit scope decision recorded with reason and user-visible consequence            |

Only Verified counts as completed. Deferred does not count as delivered. Do not use Verified for mock-only provider behavior that requires live acceptance. Record partial delivery under In progress or In validation, with remaining work identified.

## Slice register

Acceptance criteria and change areas are in the [implementation plan](implementation-plan.md). Dependencies here mirror that plan.

| ID    | Slice                           | Depends on                                   | Status        | Evidence / next action                                                                                                                                                     |
| ----- | ------------------------------- | -------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V2-00 | Formats and production baseline | None                                         | In progress   | [Technical baseline](production-baseline.md); Money Made Clear selected; measured walkthrough pending                                                                      |
| V2-01 | Revision safety                 | V2-00                                        | In progress   | Local revisions, media reuse, estimates and Short rebasing implemented; authenticated validation remains                                                                   |
| V2-02 | Recoverable script editing      | V2-01                                        | In validation | Autosave, local recovery, conflict review and atomic approval implemented; authenticated validation remains                                                                |
| V2-03 | Script-to-scene fidelity        | V2-01                                        | In validation | Implemented, unit/integration/live-provider tested and deployed to prod (20260909.1); only the browser observation of the review panel remains, blocked by command policy  |
| V2-04 | Channel profiles                | V2-01                                        | In validation | Channel identity, project assignment, availability and settings UI implemented and tested; publish-target derivation and project-settings assignment UI remain             |
| V2-05 | Formats and idea backlog        | V2-04                                        | In validation | Versioned presets, project snapshots, inheritance summary and idea backlog implemented and tested; management UIs remain                                                   |
| V2-06 | Production queue/readiness      | V2-01, V2-04, V2-05                          | In validation | Derived readiness, queue with filters/pagination and the dashboard spend-coverage fix delivered and tested; browser review pending                                         |
| V2-07 | Guided workspace                | V2-02, V2-03, V2-06                          | In validation | Overview, grouped scene and assembly contexts with shared view state, and caption review in assembly delivered and tested; browser review pending                          |
| V2-08 | Persistent release package      | V2-02, V2-04                                 | In validation | Per-destination packages, revisions, staleness, confirmation and the publish-panel metadata save delivered and tested; browser review pending                              |
| V2-09 | YouTube release completion      | V2-08                                        | Not started   | Verify scopes and design resumable finishing steps                                                                                                                         |
| V2-10 | Scheduling and calendar         | V2-06, V2-09                                 | In progress   | Scheduled release intent, zone-correct conversion, atomic claim and the sweeper task delivered and tested; worker not deployed, no provider-native scheduling, no calendar |
| V2-11 | Editorial evidence review       | V2-03, V2-05                                 | Not started   | Define manual source and claim-review workflow                                                                                                                             |
| V2-12 | Speech-aligned captions         | V2-01, V2-07                                 | Not started   | Evaluate timing fixtures and provider cost/access                                                                                                                          |
| V2-13 | Visual assembly                 | V2-01, V2-05, V2-07                          | Not started   | Implement selected footage/stills and programmatic financial graphics                                                                                                      |
| V2-14 | Audio consistency/mix           | V2-01, V2-05, V2-13                          | Not started   | Money Made Clear music bed and selective effects selected                                                                                                                  |
| V2-15 | Derived Shorts                  | V2-08, V2-12, V2-13, V2-14                   | Not started   | Extend existing editor against versioned media tracks                                                                                                                      |
| V2-16 | Production measurement          | V2-06, V2-10                                 | Not started   | Reconcile metric definitions with available usage/events                                                                                                                   |
| V2-17 | YouTube performance refresh     | V2-04, V2-10                                 | Not started   | Remove video-only marketing gates and verify token refresh                                                                                                                 |
| V2-18 | Performance-informed planning   | V2-05, V2-16, V2-17                          | Not started   | Define comparable cohorts and evidence-linked ideas                                                                                                                        |
| V2-19 | Release rehearsal               | V2-00–V2-18 delivered or explicitly deferred | Not started   | Execute validation protocol after dependent delivery                                                                                                                       |

## Per-slice delivery record

### V2-00 — Formats and production baseline

- Owner: Codex; creator confirmed Money Made Clear and its format on 2026-09-08.
- Status: In progress. Started/last updated: 2026-09-08; not verified.
- Delivered: reusable 40-scene fixture, 45-second derived Short, caption/framing/render-snapshot checks, individual failure blockers, and characterization of the current scene-edit command. Manual draft-loss and interrupted-upload scenarios are specified but not yet executed.
- Evidence: [baseline record](production-baseline.md), [fixture](../../lib/test-utils/version-two-production-fixture.ts), [assembly checks](../../lib/test-utils/version-two-production-baseline.test.ts), [revision characterization](../../db/commands/scene-revision-baseline.test.ts).
- Architecture: test-only factory uses existing domain contracts; scene-command persistence is mocked. No production implementation changed and no real account data is copied into fixtures.
- Tests: new suite 7/7 passed; broader focused suite 141/141 passed across 25 files, including the new tests. Formatting executed. `npm run typecheck` passed. `npm run lint` completed with zero errors and the existing `IDEA_PLATFORMS` unused-variable warning in `packages/prompts/src/idea-generation.ts`. Database integration and authenticated production E2E were not run; this mock-backed baseline does not verify them. Production build not applicable to test/documentation-only changes.
- Remaining: authenticated creator walkthrough with hands-on/wait/cost measurements, exact voice/media selection, and output inspection. Channel and format-specific media decisions are resolved. No live render/provider/database acceptance claimed.
- Migrations/backfill/dependencies/environment changes: none. Deployment: not deployed; test/documentation only. Rollback: remove the added test/fixture files and baseline documentation if no longer needed; no data rollback required.
- Historical baseline: characterization reproduced the 39-scene invalidation defect. V2-01 has now replaced those assertions with no-op/local revision checks; see its delivery record below.
- Production impact: not measured. The creator walkthrough remains pending and does not block V2-01 engineering.

### V2-01 - Revision safety (first increment)

- Owner: Codex. Started / updated: 2026-09-08. Status: **In progress**, not Verified.
- Delivered: unchanged content produces zero writes; changed content versions only the target scene. Later scene IDs, approvals, media, caption keys and framing references remain untouched. Storyboard timing is projected from current durations without rewriting old versions. Copying the cast retains stage slots and the speaker flag.
- Persistence: one PostgreSQL statement gates the revision and cast copy on an optimistic, workspace/project-scoped claim. A competing save receives a conflict. No generation, usage reservation, or old render mutation is performed.
- Editor: explains the current save impact and separate generation confirmation, disables unchanged saves, and reports no-op success accurately.
- Shorts: legacy absolute ranges require review/resave after a source or preceding scene revision before rendering. This is deliberately conservative until explicit compatible bindings and relative clip timing exist; it does not claim automatic derivative rebasing.
- Evidence: [command](../../db/commands/save-scene-revision.ts), [command tests](../../db/commands/scene-revision-baseline.test.ts), [content/timing tests](../../lib/domain/scene-revision.test.ts), [PostgreSQL integration](../../db/integration/project-cast-postgres.integration.test.ts), [Short guard tests](../../lib/shorts/short-revision-safety.test.ts).
- Checks: formatting completed; TypeScript passed; lint passed with the existing IDEA_PLATFORMS warning. Focused suite: 91/91 tests in 18 files passed. Real database integration: 1/1 selected test passed, including two competing saves, rollback after a failed insert, tenant isolation, preserved history, copied staging, and downstream timing after a duration edit; two unrelated tests were filtered out. Initial sandbox connection failed with EACCES; network-enabled retry and fixture cleanup passed. Production build passed (61 static pages generated); Next emitted localstorage-file runtime warnings. Authenticated browser review was not executed.
- Remaining at the end of the first increment: compatible media bindings and derivatives, estimates, Short rebasing and authenticated review. See the second increment for current status.
- Migrations/backfill/dependencies/environment configuration: none. Integration used a temporary isolated database workspace and cleaned it up. Deployment: not deployed. Rollback: revert this increment; no schema rollback.
- Creator hands-on savings remain unmeasured. The next increment below implements compatible media bindings.

### V2-01 - Compatible media reuse (second increment)

- Owner: Codex. Updated: 2026-09-08. Slice status: **In progress**, not Verified.
- Delivered: narration-only edits preserve approved images and saved framing; visual-only edits preserve approved narration and caption overrides; estimated-duration edits preserve both. Mixed edits invalidate the corresponding media. Compatible bindings copy across repeated revisions without duplicating generation or usage records.
- Selection: native approved replacements win over inherited media at the same size. Pending replacements leave inherited media available. Stale cover framing and outpaints from replaced sources cannot keep the older source selected. Image detail and audio workspace expose reuse; old-version review mutations are blocked.
- Architecture: explicit `scene_revision_media` links are created inside the atomic revision statement, scoped to workspace/project/version with foreign keys. Original prompts, generation identity, billing history and previous versions remain unchanged. No guessed backfill or provider call occurs on save. This policy is channel/genre independent.
- Migrations generated/applied to configured database: `20260908113623_scene-revision-media`, `20260908114347_revision-media-cascade-order`, `20260908114509_revision-media-source-cleanup`. The latter migrations correct source-link deletion behavior found during real cleanup tests; applied migrations were not edited. Deployments must apply all three. Source deletion cascades removal of its reuse links.
- Evidence: [reuse repository](../../db/repositories/scene-revision-media.repository.ts), [compatibility tests](../../lib/domain/scene-revision.test.ts), [selection tests](../../db/repositories/scene-revision-media.test.ts), [PostgreSQL scenarios](../../db/integration/scene-media-upload-postgres.integration.test.ts), [render selection tests](../../lib/subtitles/resolve-scene-image.test.ts).
- Validation: four live PostgreSQL scenarios passed, including repeated revisions, native replacement, original-history preservation, cross-workspace lookup refusal, caption overrides, framing and workspace cleanup. A targeted duration-case rerun passed with historical image/audio review rejection assertions. Initial tests exposed a cleanup constraint failure; four exact temporary workspaces/users were verified and removed after correction. `npx vitest run` on the focused revision/media/fixture directories passed 112 tests in 22 files; the final targeted selection/compatibility/renderer run passed 18 tests in 3 files, including two additional renderer regression cases (these runs overlap). `npm run typecheck` passed after correcting a nullable test fixture. Formatting passed; full lint and final targeted lint passed with only the existing IDEA_PLATFORMS warning in the full run. Final `npm run build` passed and generated 61 pages after a network-enabled retry for Google Fonts; existing localstorage-file warnings remain. No live provider generation was tested.
- Browser: the available Chrome tab remains on Clerk sign-in; authenticated editor interaction was not verified. No real image/audio/video output was generated.
- Remaining slice acceptance: exact regeneration estimates before save; automatic compatible Short rebasing; authenticated editor/production walkthrough. The existing conservative Short guard remains active.
- Dependencies/environment variables: none. Backfill: none. Deployment: not deployed; the code was subsequently pushed to origin/master. Rollback: revert application changes while retaining the additive table/migrations; legacy queries remain usable. No binary assets or historical generations were rewritten.
- Next action: finish the remaining V2-01 estimates and Short preservation before V2-02. Production savings remain unmeasured.

### V2-01 - Edit estimates and Short anchors (third increment)

- Owner: Codex. Started: 2026-09-08; updated: 2026-09-09. Slice status: **In progress**, not Verified; this record supersedes the remaining engineering items in the second increment.
- Delivered: the scene editor reviews affected media and replacement estimates before an explicit save. It calls a read-only, authorized server action; estimates use current configured rates, the edited prompt and the original approved image quality/style/references. Speech estimates use normalized replacement narration. Uploaded/recorded assets, missing settings and narration beyond provider limits are marked unpriced. Changing a field clears the estimate; preview and recoverable request errors preserve uncontrolled form inputs. Saving never reserves usage or invokes a provider.
- Estimate scope: one replacement per affected approved scene image and narration track. Outpainting, re-rendering, retries and media not yet approved are explicitly excluded. Generation performs its own authoritative estimate and budget checks later. No guaranteed invoice or automatic replacement is implied.
- Shorts: new and explicitly resaved compositions capture source-relative trim offsets plus the approved audio generation identity from the server's validated timeline. The editor, preview and export use the same read-time projection. Earlier timing changes and source revisions with the same approved audio preserve clip content. Replaced audio, removed sources and trims outside the new duration require review. Applying trim edits selects the current source revision so reviewed clips can be saved again.
- Architecture: audio identity is a historical snapshot, not a foreign key or an asset-access capability; it remains available for comparison if the audio is removed. The source scene/version retain their existing tenant foreign keys. Original saved absolute ranges, generation records, costs and historical render snapshots are unchanged. Legacy clips have no guessed anchors and keep conservative review rules until explicitly resaved. Policies remain genre independent.
- Migration: generated, reviewed and applied `20260908204936_short-relative-anchors`; nullable columns plus a constraint enforcing complete anchors and positive ranges. No backfill, dependency, environment-variable or paid-provider changes. Deploy the web application only after applying the migration. Deployment: not deployed. Push: **this increment was pushed** — it is contained in commit 20f8984 on origin/master (superseding the earlier "not pushed" statement in this record). Rollback: retain the additive columns/migration and revert application changes; old code can read the original absolute ranges.
- Evidence: [anchor projection](../../lib/shorts/short-anchors.ts), [anchor tests](../../lib/shorts/short-anchors.test.ts), [estimate service](../../lib/scenes/scene-revision-estimate.ts), [estimate tests](../../lib/scenes/scene-revision-estimate.test.ts), [PostgreSQL persistence tests](../../db/integration/short-composition-postgres.integration.test.ts).
- Checks: `npx vitest run lib/shorts lib/scenes/scene-revision-estimate.test.ts lib/test-utils lib/schemas/short.test.ts lib/domain/scene-revision.test.ts db/commands/scene-revision-baseline.test.ts lib/render lib/costs lib/subtitles` passed 179 tests across 34 files. Final targeted estimate/anchor run passed 15 tests across two files, including four added edge cases (these runs overlap). `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/short-composition-postgres.integration.test.ts` passed both live PostgreSQL tests with fixture cleanup, including anchor persistence, rejected partial anchors and cross-workspace update refusal. `npm run typecheck` passed. Full lint passed with zero errors and the existing IDEA_PLATFORMS warning; final changed-file lint passed. Prettier write/check and `git diff --check` passed. Final `npm run build` passed after the draft-preservation fix, including TypeScript and 62 generated pages; existing localstorage-file warnings remain. No billable provider or live output test was performed.
- Browser: runtime setup succeeded but returned no connected browsers. Authenticated editor/preview/export interaction was not executed. No real media was generated or inspected.
- Remaining: authenticated creator walkthrough and final release validation; measured production savings remain unverified. Next planned feature slice is V2-02 recoverable script editing, following the V2-01 review.

### V2-02 - Recoverable script editing and approval

- Owner: Codex. Started/updated: 2026-09-09. Status: **In validation**, not Verified. User authorized the next slice while V2-01 authenticated review remains outstanding.
- Delivered: one-second debounced autosave with visible status, manual retry and reconnect handling; typing continues while a save is in flight. Late responses update the saved base, never overwrite newer typing. A fifteen-second confirmation deadline ends a stalled UI request; it does not claim to cancel a possibly committed database operation.
- Local recovery: only text, base revision and timestamp are stored, keyed by application user/workspace/project/tab session. Up to twenty records survive for at most seven days. Reload/navigation offer restore/discard; stale recovered revisions enter explicit conflict review. Storage failures are visible. Explicit discard and application sign-out clear recovery. No tokens, signed URLs or session credentials are stored in these records. Browser/tab-session loss and cross-device recovery are not guaranteed.
- Editorial flow: generated script insertion is staged for review, preserving existing writing until explicit replacement. Conflicts display the local editor and newer server text, with explicit choices guarded by a fresh optimistic revision on the next write. Viewers cannot edit or approve, and archived/missing projects are refused by the new action.
- Approval: a single atomic PostgreSQL statement claims the expected draft revision, writes exact submitted text/statistics, supersedes the old approval and inserts an immutable approved version. Retrying a lost approval response resolves its existing snapshot. Version creation/restoration now use the same gated snapshot path; losing a concurrent claim cannot create an extra historical version. Existing approved-version history controls remain available when the editor is settled.
- Migration generated/reviewed/applied: `20260908214436_script-draft-snapshots`; nullable positive `source_draft_revision` with per-project uniqueness. No historical backfill or inferred revision identity. No dependencies, environment variables, provider spending or Trigger tasks added. Shared script-command callers retain their full version return shape.
- Evidence: [controller](../../lib/scripts/script-draft-controller.ts), [recovery](../../lib/scripts/script-recovery.ts), [server action tests](../../lib/scripts/script-draft-action.test.ts), [atomic command](../../db/commands/commit-script-version.ts), [PostgreSQL tests](../../db/integration/script-draft-postgres.integration.test.ts), [authenticated scenario](../../e2e/script-draft.authenticated.spec.ts).
- Validation: `npx vitest run lib/scripts lib/auth lib/policies lib/schemas/project.test.ts lib/domain/script-statistics.test.ts lib/domain/script-version-deletion.test.ts` passed **45 tests across 11 files**. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/script-draft-postgres.integration.test.ts` passed **3/3** with fixture cleanup, covering exact/idempotent approval, approval replacement, competing claims, stale writes, tenant scoping and atomic restore. `npm run typecheck` passed; final `npm run build` passed including TypeScript and 62 generated pages. Full lint passed with only the existing IDEA_PLATFORMS warning; final targeted lint passed. Prettier write/check and `git diff --check` passed after correcting two test-file formatting discrepancies. Existing build localstorage-file warnings remain. No live provider calls or browser-output verification were performed.
- Browser: one connection setup attempt timed out after five seconds and was terminated; no retry loop. Authenticated Playwright scenario is authored but unexecuted. No live production walkthrough, generated media or measured creator savings are claimed.
- Deployment: not deployed. Push: **this slice was pushed** — it is contained in commit 20f8984 on origin/master (superseding the earlier "not performed" statement). Apply the migration before web/worker rollout because script version rows gained a field and shared version commands changed. Rollback: revert this increment while retaining the additive migration/column; historical content and original versions remain intact.
- Next action: authenticated workflow review, then V2-03 script-to-scene fidelity. Keep V2-01/02 validation limitations visible until exercised.

### V2-03 — Verified script-to-scene fidelity

- Owner: Claude. Started / last updated: 2026-09-09. Status: **In validation**, not Verified.
- Scope delivered: concatenated scene narration is validated against the approved script after generation and before it can become the active scene plan. A documented normalization policy limits comparison-time changes to Unicode NFC, CR/CRLF to LF, zero-width removal, whitespace-run collapse and boundary trimming, with each rule justified in the module; case, punctuation, quotes, dashes, numerals and wording are never normalized. Missing, duplicated, reordered, invented and empty narration, plus an uncovered script tail, are rejected with a single actionable discrepancy naming the scene number, script character offset, the approved excerpt and the received excerpt. A difference that is purely punctuation is flagged `punctuationOnly` and reported rather than folded away. One bounded repair attempt re-prompts the provider with the discrepancy quoted back. Paid output that fails validation records provider request id, tokens and actual cost and reconciles its reservation; no scenes are written, so the previously usable plan is preserved. The scenes page compares approved-script narration to the current scene plan and marks the first divergence.
- Acceptance criteria met (plan V2-03): missing/duplicated/reordered/invented narration fails before becoming the active plan — met, unit-tested and enforced in the worker. Multilingual, punctuation, repeated-phrase and boundary-whitespace cases — met (Japanese, Arabic, decomposed Spanish, German wording change, smart quotes, changed question mark, repeated phrases, leading/trailing whitespace). Failed analysis preserves the previous usable plan and reports an actionable discrepancy — met, evidenced by a live PostgreSQL test asserting the prior three scenes and their narration survive a rejected run. Provider usage recorded when paid output fails validation, with bounded repair under existing budget/reservation/idempotency rules — met for recording and bounding; see the budget caveat below. Rendered scene narration comparable to the approved script during review — met via the coverage panel.
- Remaining criteria / next action: authenticated creator walkthrough of the coverage panel and of a real rejected run. No live provider generation was executed, so the repair prompt has never been exercised against a real model. Budget caveat: the preflight budget check in `startSceneAnalysis` is unchanged and still sizes a single call, so a run that uses its one repair can reconcile above its reserved amount, bounded at roughly twice the estimate. Reservations already reconcile to actual cost and nothing is spent without a reservation, but the estimate is not yet repair-aware; revisit if repair rates prove non-trivial in production.
- Architectural decisions and rationale: (1) Forward cursor matching rather than per-passage search, so a script that legitimately repeats a phrase passes while a plan covering one occurrence twice and the other not at all fails — a search-based check accepts exactly that corruption. (2) NFC and not NFKC, because compatibility folding rewrites `½` to `1/2` and `ﬁ` to `fi`, which changes what is written. (3) The walk stops at the first divergence; after coverage breaks there is no reliable resynchronization and downstream differences would be noise rather than an actionable report. (4) `failSceneAnalysisWithUsage` is separate from `failSceneAnalysis` because the existing path releases the reservation at zero, which is correct only when nothing was generated. (5) The review comparison is recomputed from current scene versions instead of stored, because scenes are editable after analysis and a stored verdict would describe a plan that no longer exists; this also removed any need for a migration. (6) `MAX_SCENE_ANALYSIS_REPAIR_ATTEMPTS` is a policy constant rather than an environment variable, so a deployment cannot silently raise billable retries. (7) The failure category lives in `lib/scenes/scene-analysis-failure.ts` so the review component can name it without importing Trigger.dev and server-only providers into the client bundle. All logic is text-structural and independent of channel, genre and language.
- Changed files: new `lib/domain/narration-normalization.ts`, `lib/domain/narration-coverage.ts`, `lib/scenes/script-coverage-view.ts`, `lib/scenes/scene-analysis-failure.ts`, `components/scenes/ScriptCoveragePanel.tsx`, their unit tests, and `db/integration/scene-analysis-fidelity-postgres.integration.test.ts`; modified `trigger/scene-analysis.ts`, `db/commands/scene-commands.ts`, `packages/prompts/src/scene-analysis.ts`, `packages/prompts/src/index.ts`, `components/scenes/ScenePlanner.tsx`, `components/scenes/SceneAnalysisErrorState.tsx` and the scenes `page.tsx`. Not committed at the time of writing.
- Commands executed and exact results: `npx vitest run lib/domain lib/scenes packages/prompts/src/scene-analysis.test.ts lib/costs lib/workflows lib/schemas/scene.test.ts` passed **228 tests across 38 files**. `npm test` passed **1398 tests across 243 files with 104 skipped** on two consecutive runs; an earlier run of the same suite reported **one failed file** whose identity was not captured and which did not reproduce on either re-run. Treat the suite as green but that single observation as an unexplained transient, consistent with the known load-dependent 5s timeout in `lib/marketing/documents/extract-stored-document.test.ts`. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/scene-analysis-fidelity-postgres.integration.test.ts` passed **3/3** against the configured Neon database. A temporary follow-up query confirmed **zero** leftover fixture workspaces and users; that check file was deleted. `npm run typecheck` passed. `npm run lint` passed with zero errors and only the pre-existing `IDEA_PLATFORMS` warning. `npm run build` compiled successfully and generated **62 pages**; existing localstorage-file warnings remain. `npx prettier --check` on every added and modified file reported all files formatted; the repository still carries pre-existing formatting debt in unrelated files (36 files flagged on master, 28 with this work applied). `git diff --check` clean.
- Browser / output review evidence: none. No authenticated browser session was attempted for this slice, and no live provider call, generated media or render was produced. The repair prompt and the coverage panel have not been observed in a running application.
- Database migrations: **none generated or applied.** `scene_analysis_runs` already stores the immutable input identity (`script_version_id`, referencing the immutable approved `project_script_versions.content`, plus `request_fingerprint`, `prompt_version`, `model` and `final_prompt`) and the safe validation result (`error_category`, `safe_error_message`) alongside `provider_request_id`, `input_tokens`, `output_tokens` and `actual_cost_cents`. No applied migration was edited.
- Backfill: none. No historical run, approved script, scene plan, generation, usage record or render was rewritten.
- Dependencies and environment variables added/changed: none.
- Deployment: not deployed and not pushed. The `scene-analysis` Trigger.dev task changed, so a worker deploy is required before validation takes effect in a deployed environment; the web application change is independent and safe to ship on its own.
- Rollback or disable procedure: revert the application changes. There is no schema or data rollback. Reverting also restores `scene-analysis-v1`, which changes the idempotency key for new runs; historical runs keep their stored `final_prompt` and remain reproducible either way.
- Known limitations / external finishing steps: validation compares narration text only and cannot detect faithful narration attached to the wrong visual. Repair is capped at one extra provider call, so a persistently unfaithful model fails the run rather than retrying indefinitely. The reservation estimate is not repair-aware. No live provider verification has been performed.
- Measured production impact: not measured. No creator walkthrough has been executed.

### V2-03 — Verified script-to-scene fidelity (second increment)

- Owner: Claude. Updated: 2026-09-09. Status: **In validation**, not Verified. This record supersedes the testing and layering items in the first increment.
- Delivered: the validate-and-repair policy moved out of the Trigger.dev task into `lib/scenes/generate-validated-scene-plan.ts` behind a one-method `SceneAnalysisGenerator` seam. The worker now only resolves state, calls the generator, and persists; the decision about whether output is acceptable and how many times it is worth paying to ask again is testable without a network call or a queue. Usage is returned on both the accepted and rejected branches, because a rejected plan still has to be reconciled. This closes a gap between the first increment's stated plan and what it actually delivered: worker-level tests with a mocked provider were promised and were missing.
- New evidence: [generator](../../lib/scenes/generate-validated-scene-plan.ts), [generator tests](../../lib/scenes/generate-validated-scene-plan.test.ts), [authenticated scenario](../../e2e/script-coverage.authenticated.spec.ts).
- Tests added: nine mocked-provider cases covering a faithful plan costing exactly one call; a rejected plan repaired on the second call with the discrepancy quoted into the repair prompt; token accumulation across both attempts (220 input / 100 output, two attempts); exhaustion of the bound returning the discrepancy plus full usage and the last request id; a raised bound making exactly three calls; repair disabled making exactly one; an over-long plan raising `OPENAI_INVALID_RESPONSE`; and a transport failure propagating as a provider fault rather than being reported as a fidelity problem.
- Commands executed and exact results: `npx vitest run lib/scenes/generate-validated-scene-plan.test.ts` passed **9/9**. `npm test` passed **1407 tests across 244 files, 104 skipped**, and then passed three further consecutive runs at **244 files** each. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/scene-analysis-fidelity-postgres.integration.test.ts` passed **3/3** again after the refactor. `npm run typecheck` passed. `npm run lint` passed with zero errors and only the pre-existing `IDEA_PLATFORMS` warning, after removing imports the refactor left unused. `npm run build` compiled successfully and generated **62 pages**. `npx prettier --check` on the changed files reported all formatted; `git diff --check` clean.
- Intermittent full-suite failure, resolved as far as evidence allows: `npm test` reported **one failed file on two separate occasions** during this slice. On the second occasion the suite was re-run with full output captured to a log and it passed; three further logged runs also passed, so the failing file was never captured and its identity remains **unknown**. Recorded here rather than attributed to a guess. Seven full-suite runs in total: two failures, five passes, and every focused, integration and build run passed.
- Browser / live evidence: still none. `curl http://localhost:3000` returned no response, and per the standing constraint the development server was not started, so the authenticated Playwright scenario is **authored but unexecuted**. It deliberately asserts only the non-billable states — panel hidden with no approved script, and hidden with an approved script but no scenes — because a faithful-plan assertion needs a real analysis run.
- Deployment readiness (read-only check, nothing deployed): `trigger.dev whoami` resolved the `default` profile against `https://api.trigger.dev`. The current **prod** worker is **20260908.2** on SDK 4.5.4 with 26 tasks registered including `scene-analysis`. That worker predates this slice, so production still runs the **unvalidated** analysis task. A worker deploy (`npm run trigger:deploy`) is required before fidelity validation takes effect for real runs; the web application half is independent and already safe to ship.
- Migrations, backfill, dependencies, environment variables: none, unchanged from the first increment.
- Remaining before Verified: deploy the worker, then execute one real analysis run to exercise the repair prompt against a live model, and run the authenticated scenario against a running application. Until then the repair prompt has never faced a real provider and the coverage panel has never been observed in a browser.
- Rollback: unchanged. Revert the application changes; there is no schema or data rollback. The extracted generator is additive and reverting it restores the previous inline loop.

### V2-03 — Live verification and production deployment

- Owner: Claude. Updated: 2026-09-09. Status: **In validation**, not Verified. One acceptance criterion still lacks its evidence; see the blocked item below.
- Live provider verification (the question unit tests could not answer): added an opt-in, cost-gated suite at [scene-analysis-live-provider.test.ts](../../lib/scenes/scene-analysis-live-provider.test.ts), run with `RUN_LIVE_PROVIDER_TESTS=true`. The risk it exists to measure is a **false rejection** — the validator refusing an editorially correct plan because the model normalized an apostrophe or a dash. Both cases passed against the configured live model on the first attempt, with **no repair needed**:
  - Plain prose, four-sentence finance script: `ok=true attempts=1 tokens=479/1494 scenes=4`, and an independent recheck of the returned narration reported `covered`.
  - A script deliberately loaded with a curly apostrophe, an em dash, `1.5%`, `$10,000` and a quoted `"safe"`: `ok=true attempts=1 tokens=479/1128`.
  - Conclusion: the v2 prompt and the strict normalization policy are compatible in practice. The validator did not fire on ordinary correct output, so the gate is not expected to block real production runs. Two runs of the suite were executed (the second only to capture diagnostics); total live cost was a few cents.
  - What this does **not** prove: the rejection path against a real model. A capable model cannot be made to drop a passage on command, so the repair prompt has still never been exercised by a genuine provider failure. Its persistence behaviour is covered by the PostgreSQL suite and its control flow by the mocked-provider suite.
- Production deployment: **deployed.** `trigger.dev deploy --env prod` built and promoted version **20260909.1** (SDK 4.5.4, 26 tasks detected, deployment `of01bhg5`), and a follow-up worker query confirms prod now serves `20260909.1`. The previous worker `20260908.2` ran the unvalidated task. One environment variable was synced by the existing `syncEnvVars` extension; no new variable was introduced. The CLI warned that 4.5.4 is behind 4.5.16 — not acted on, since a CLI upgrade is unrelated to this slice.
- Remaining criterion and why it is outstanding: acceptance item five, _rendered scene narration and the approved script can be compared during review_, has implementation and unit evidence ([view-model tests](../../lib/scenes/script-coverage-view.test.ts)) but **no rendered observation**. The authenticated scenario is authored at [script-coverage.authenticated.spec.ts](../../e2e/script-coverage.authenticated.spec.ts) and could not be executed: both `npm run dev` and `npx playwright test` were refused by the environment's command permission policy, which the creator's standing authorization does not override. This is an environmental block, not an outstanding engineering task. To clear it, allow those two commands and run `npx playwright test e2e/script-coverage.authenticated.spec.ts`; every required variable (`E2E_CLERK_USER_EMAIL`, `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`) is already present in `.env`.
- Commands executed and exact results: `RUN_LIVE_PROVIDER_TESTS=true npx vitest run lib/scenes/scene-analysis-live-provider.test.ts` passed **2/2**, twice. Trigger.dev deploy succeeded and promoted `20260909.1`. `npm run dev` and `npx playwright test` were **blocked by the command permission classifier** and did not run.
- Measured production impact: still not measured. No creator walkthrough and no production scene analysis has been observed since the deploy.

### V2-04 — Channel production profiles

- Owner: Claude. Started / last updated: 2026-09-09. Status: **In validation**, not Verified.
- Scope delivered: workspace-owned production channels that exist independently of OAuth. A channel carries name, platform, audience, tone, language, release cadence, IANA time zone and optional aspect-ratio and budget defaults. Projects gained an optional channel assignment, project creation accepts and validates one, and workspace settings gained a **Production channels** section listing each channel with its live publish availability and project count. Archived, never deleted.
- Central architectural decision — **no stored connection id**: a profile records only the platform's own `external_account_id`, and the live grant is resolved by joining on (workspace, platform, external account id) at read time. Pointing a profile at a `platform_connections` row would tie production identity to an OAuth grant that revocation deletes and reconnection recreates under a new id. With the identity join, a revoke leaves the channel and its entire project history untouched and merely reports `disconnected`, and a reconnect re-links automatically with no repair step. This is proven end to end by a live PostgreSQL test that connects, deletes the connection row outright, and reconnects.
- Second decision — **the composite foreign key had to be reworked**. The first generated migration used `ON DELETE SET NULL` on composite keys `(connection_id, workspace_id)` and `(channel_profile_id, workspace_id)`. PostgreSQL nulls _every_ column in a composite SET NULL, and `workspace_id` is NOT NULL, so deleting a connection or a profile would have failed outright at runtime. That migration was discarded before it was applied. The connection key was removed entirely by the identity-join design above, and the project key is now `ON DELETE CASCADE`, which is only safe because channel profiles are archived and never hard-deleted — there is deliberately no delete command, and the schema comment says so. Workspace deletion still cleans up through the existing workspace cascade, verified by fixture teardown leaving zero rows.
- Acceptance criteria: _multiple channels keep distinct profiles and project lists_ — met, live test asserts two channels plus an unassigned backlog resolve to disjoint project lists. _New projects inherit the intended channel; unassigned legacy projects remain usable_ — met, `channel_profile_id` is nullable, creation validates the channel against the workspace, and an unassigned project is exercised in the same test. _Profile assignment and defaults cannot reference another workspace's resources_ — met at two levels: the command refuses, and a raw update bypassing the command is rejected by the database itself, both asserted. _Reconnecting or revoking OAuth preserves production identity and history while blocking unavailable publication actions_ — met for identity, history and the availability state machine; `canChannelPublish` is only true for `ready`. _Upload targets come from explicit project/release selection, never an unrelated first active connection_ — **met**. The defect was real and it was in `selectInitialPublishingTarget`, which fell back to `connections.find(status === "active")` — whichever active account sorted first. It now follows an explicit order: an in-flight publication wins, then the project's assigned channel, and only then a sole active account. With two or more accounts connected and no channel assigned it selects nothing and makes the user choose rather than guessing. `loadPublishingView` resolves the channel's live connection and passes it through, and six tests cover the order, including a channel whose account is revoked, a channel id absent from the list, and an in-flight publication overriding the channel. An existing test that asserted the old first-active fallback was rewritten, with the reason recorded in the test itself. `findActivePlatformConnection` still exists and is used only by Marketing Studio auto-scheduling, out of scope by standing instruction.
- Remaining criteria / next action: none outstanding in code. Both items previously listed here are delivered — the publish panel derives its target from the assigned channel, and project settings gained a **Production channel** section bound to `assignProjectChannelAction` with an explicit "No channel" option and a warning when the selected channel's account is disconnected. Authenticated browser review remains blocked by the environment's command permission policy, exactly as for V2-03; that is the only evidence gap left in this slice.
- Evidence: [schema](../../db/schema.ts), [repository](../../db/repositories/channel-profiles.repository.ts), [commands](../../db/commands/channel-profile-commands.ts), [availability](../../lib/channels/channel-availability.ts), [availability tests](../../lib/channels/channel-availability.test.ts), [input schema tests](../../lib/schemas/channel-profile.test.ts), [PostgreSQL scenarios](../../db/integration/channel-profiles-postgres.integration.test.ts).
- Commands executed and exact results: `npx vitest run lib/channels lib/schemas/channel-profile.test.ts` passed **18/18**. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/channel-profiles-postgres.integration.test.ts db/integration/scene-analysis-fidelity-postgres.integration.test.ts` passed **10/10** against the configured Neon database, and a follow-up query confirmed zero leftover fixture workspaces, users and channel rows. `npm test` passed **1425 tests across 246 files**, then four further consecutive full runs passed at 246 files each. `npm run typecheck` passed. `npm run lint` passed with zero errors and only the pre-existing `IDEA_PLATFORMS` warning. `npm run build` compiled successfully. `prettier --check` on every added file reported all formatted; `git diff --check` clean.
- Intermittent full-suite failure: `npm test` again reported **one failed file on one occasion** during this slice (245 passed instead of 246). Six subsequent runs passed, including a four-run capture loop, and the failing file was never identified. This is the third sighting across the session, in roughly fifteen full-suite runs, and it predates both V2-03 and V2-04. Recorded as an open, unattributed flake rather than guessed at.
- Database migrations: two, both generated and applied to the configured database. `20260909003939_typical_piledriver` adds `channel_profiles`, the `channel_cadence` and `channel_profile_status` enums, `projects.channel_profile_id`, its index and the tenant-scoped composite foreign key. `20260909004940_premium_scarlet_spider` adds three `audit_action` enum values. `npm run db:migrate` remains broken by pre-existing journal drift, so both were applied with `drizzle-kit push --force` and then hand-verified against `information_schema` and `pg_constraint`: all nineteen columns, the composite foreign key definition and both CHECK constraints were confirmed present with the intended definitions. An earlier generated migration was discarded before application; no applied migration was edited.
- Backfill: none, and deliberately none. No historical project was assigned to a channel, because guessing would attribute real production history to an arbitrary account.
- Dependencies and environment variables added/changed: none.
- Deployment: not deployed. The migrations are applied to the configured database; the web application change is not released. No Trigger.dev task changed in this slice, so no worker deploy is required for it.
- Rollback: revert the application changes and leave the additive table, column and enum values in place; every existing query ignores them. If the table must go, archive first — dropping `channel_profiles` would cascade to `projects` through the tenant foreign key.
- Known limitations: channel defaults are stored but not yet applied to new projects created from a channel. `Intl` time-zone validation accepts any zone the runtime knows, which on an outdated ICU build could differ from the deployment target.
- Measured production impact: not measured.

#### V2-04 second increment — publish target and project assignment (2026-09-09)

- Delivered the two items the first increment left open. `selectInitialPublishingTarget` no longer falls back to the first active connection; project settings gained a **Production channel** control. Nine tests added or rewritten across `lib/publishing` and the new component's action path.
- Commands: `npx vitest run lib/publishing` passed **140/140**. `npm test` passed **1431 tests**. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/channel-profiles-postgres.integration.test.ts` passed **7/7**. `npm run typecheck`, `npm run lint` (one pre-existing warning) and `npm run build` passed.
- Behaviour change to note on rollout: a workspace with several connected accounts and a project with no assigned channel now opens the publish panel with **no account selected** instead of a guessed one. That is the intended fix, and it makes the first publish after upgrade require an explicit choice.
- Not deployed. No schema, migration, dependency or environment change in this increment.

### V2-05 — Reusable formats and channel idea backlog

- Owner: Claude. Started / last updated: 2026-09-09. Status: **In validation**, not Verified.
- Scope delivered: a small versioned format preset (audience, editorial structure, target duration, aspect ratio, frame rate, voice and style references, captions, budget default), an idea backlog organised by channel, format, priority and planned release, format selection in the create-project flow with an inheritance summary, and repeat-use history for saved ideas.
- Central decision — **a preset is an identity, its versions are immutable**. `format_presets` holds only name, slug, optional owning channel and status; every editable value lives in a `format_preset_versions` row that is never updated after insert. Editing a format appends a version; a project stores `format_preset_version_id`, the exact version it was created from. This is what makes "editing a preset never silently changes in-progress videos" true by construction rather than by convention, and it is asserted directly: a live test creates a project against v1, publishes v2 with a different budget and duration, and confirms the project still resolves v1 with its original values while a new project would inherit v2.
- Second decision — **`NO ACTION` rather than composite `SET NULL`**, applying the lesson from V2-04. PostgreSQL nulls every column of a composite `SET NULL`, and `workspace_id` is `NOT NULL`, so that form makes deletes fail outright. The project→version and idea→channel/format keys are therefore plain `NO ACTION`: the check is deferred to end-of-statement, so deleting a version a project cites is refused (asserted), while a workspace cascade that removes both still succeeds (proven by fixture teardown leaving zero rows across five tables).
- Acceptance criteria: _a creator starts a recurring video with inherited settings and sees which values were inherited or overridden_ — met; `resolveFormatInheritance` compares submitted values against the exact inherited version and the create-project form renders a per-field summary marking each value inherited, overridden (with the format's value alongside) or unset. _Projects snapshot the preset version; editing a preset never silently changes in-progress videos_ — met, live PostgreSQL evidence above. _Existing saved-idea-to-project creation is reused, with duplicate-use history visible rather than silently preventing deliberate follow-ups_ — met; the existing `ideaId` path is unchanged and now records `source_content_idea_id`, and `listProjectsFromIdea` returns every project made from an idea, with a test creating two deliberate follow-ups from one idea and finding both. _Channel defaults never bypass cost confirmation, maximum limits, or role checks_ — met; the preset budget is validated against the same 100000-cent ceiling the project form enforces, asserted by a test that probes both schemas with the same over-limit value, and generation still performs its own estimate, reservation and confirmation because nothing in this slice touches that path. _Manual ideas and blank projects remain supported; no paid idea-generation expansion is required_ — met; a blank project with no format and no idea is created and asserted to store nulls for both, and no provider call was added.
- Remaining: no per-slice code gaps identified against the plan's criteria. A format management UI (create/edit/archive presets in settings) is not built — presets are currently created through the command layer — and the backlog fields have schema, validation and indexes but no editing surface yet. Both are usability follow-ups rather than acceptance criteria. Authenticated browser review remains blocked by the environment's command permission policy.
- Evidence: [schema](../../db/schema.ts), [commands](../../db/commands/format-preset-commands.ts), [repository](../../db/repositories/format-presets.repository.ts), [inheritance](../../lib/formats/format-inheritance.ts), [inheritance tests](../../lib/formats/format-inheritance.test.ts), [schema tests](../../lib/schemas/format-preset.test.ts), [PostgreSQL scenarios](../../db/integration/format-presets-postgres.integration.test.ts), [summary component](../../components/projects/FormatInheritanceSummary.tsx).
- Commands executed and exact results: `npx vitest run lib/formats lib/schemas/format-preset.test.ts lib/ideas lib/publishing` passed **173 tests across 29 files**. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run db/integration/format-presets-postgres.integration.test.ts db/integration/channel-profiles-postgres.integration.test.ts` passed **14/14**, and a follow-up query confirmed zero leftover workspaces, users, presets, versions and channels. `npm test` passed **1451 tests across 246 files**. `npm run typecheck` passed. `npm run lint` passed with zero errors and only the pre-existing `IDEA_PLATFORMS` warning. `npm run build` compiled successfully. `git diff --check` clean.
- Intermittent full-suite failure: seen once more during this slice (1450 passing instead of 1451), passing on the immediate re-run. **New observation worth recording**: the failing runs are consistently the slow ones — the failing run took 194s against 104s for a passing run of the same suite — which points at a load-dependent timeout rather than a logic fault. Four sightings across roughly twenty full-suite runs this session; still not attributed to a specific file, and it predates V2-03, V2-04 and V2-05.
- Database migration: `20260909012407_numerous_squirrel_girl`, generated and applied with `drizzle-kit push --force` (`npm run db:migrate` remains broken by pre-existing journal drift) and reviewed line by line before application, specifically to confirm no composite `SET NULL` survived. Adds `format_presets`, `format_preset_versions`, the `format_preset_status` enum, `projects.format_preset_version_id`, `projects.source_content_idea_id`, and four backlog columns on `content_ideas` with their tenant-scoped keys and indexes. No applied migration was edited.
- Backfill: none. No existing project was given a format, and no existing idea was assigned a channel or priority beyond the zero default.
- Dependencies and environment variables added/changed: none.
- Deployment: not deployed. The migration is applied to the configured database; no Trigger.dev task changed, so no worker deploy is required for this slice.
- Rollback: revert the application changes and leave the additive tables, columns and enum in place; existing queries ignore them. Dropping `format_preset_versions` would be refused while any project cites a version, which is the intended protection.
- Known limitations: format and backlog management UIs are not built (above). Voice and style references are stored as ids but not yet applied to a created project's generation defaults. `content_ideas.priority` is a plain integer with no agreed scale yet.
- Measured production impact: not measured.

Copy this template below for each slice when work begins. Link commits/PRs and repository evidence rather than pasting secrets, signed URLs, raw credentials, or private channel content.

```markdown
### V2-06 - Production queue and derived readiness

- Owner: Claude. Started / last updated: 2026-09-09. Status: **In validation**, not Verified.
- Scope delivered: a production queue at `/app/queue` listing videos by channel and intended release with derived blockers, review counts and a next action; a pure readiness resolver; persisted editorial intent (`projects.planned_release_at`) with a settings control; link-based tenant-scoped filters with bounded pagination; and a correction to the dashboard's spend coverage.
- Central decision - **readiness is evidence, never a label**. Project settings lets a creator set `projects.status` by hand, so `ProjectProductionFacts` carries only counts of things that happened and offers nowhere to put a status. A test asserts that absence directly, and a live-database test sets a project to `completed` with nothing produced and confirms it still reports `script_missing` with a next action of "Open the script". Editorial status travels beside readiness as intent and is never an input to it.
- Second decision - **editorial intent never advances a project**. `planned_release_at` orders the queue and produces the `scheduled` release state, but a real render or publication always outranks it, so scheduling a video cannot make it look finished. Asserted both as a unit case and against PostgreSQL.
- Acceptance criteria: _a project can show "3 images awaiting review" or "Render failed" with the exact recovery link_ - met; both strings are asserted verbatim and each blocker and review carries its own project-relative recovery href. _Published, rendered and scheduled are distinct states; manual Settings status cannot manufacture readiness_ - met, with the live evidence above. _A partial batch failure remains visible and cannot produce a generic success badge_ - met; `isComplete` requires a succeeded render **and** zero blockers **and** zero reviews, failed images are reported separately from images never requested, and a failed render stays visible until one actually succeeds. _Queue filters are paginated and tenant-scoped; dashboard queries remain bounded_ - met; the page size is capped at 50 with unknown values falling back rather than failing the page, per-project aggregates are filtered by both workspace and the page's own project ids, and a cross-workspace test confirms another workspace's projects never appear. _Spend labels accurately identify their coverage and period_ - met by fixing a real defect, below.
- **Defect found and fixed**: the dashboard's "Spend to date" card summed only `scene_image_generations.actual_cost_cents` while being presented as overall spend. It understated production cost by everything else on the ledger - script and scene analysis, narration audio, renders, titles and thumbnails - and contradicted the Usage page. It now reuses `getWorkspaceUsageSummary` and is labelled "Production spend / All billable operations, month to date".
- **Two defects caught by live-database tests, not by unit tests**: an `ORDER BY` that emitted `nulls last asc` instead of `asc nulls last`, which failed every queue query outright; and a script signal that counted the empty draft row seeded at project creation as written work, which made "no script yet" unreachable and mislabelled every new project as having an unapproved draft.
- Remaining: no per-slice code gaps identified against the plan's criteria. Authenticated browser review remains blocked by the environment's command permission policy, so the queue has not been observed rendering.
- Evidence: [resolver](../../lib/production/production-readiness.ts), [resolver tests](../../lib/production/production-readiness.test.ts), [repository](../../db/repositories/production-queue.repository.ts), [filters schema](../../lib/schemas/production-queue.ts), [PostgreSQL scenarios](../../db/integration/production-queue-postgres.integration.test.ts), [queue page](../../components/production/ProductionQueuePageContent.tsx).
- Commands executed and exact results: `npx vitest run lib/production lib/schemas/production-queue.test.ts` passed **33 tests across 3 files**. `RUN_DATABASE_INTEGRATION_TESTS=true npx vitest run` over the production-queue, format-presets, channel-profiles and scene-image-batch suites passed **25/25**, with a follow-up query confirming zero leftover fixture workspaces, projects and users from this slice. `npm test` passed **1484 tests across 251 files**. `npm run typecheck` passed. `npm run lint` passed with zero errors and only the pre-existing `IDEA_PLATFORMS` warning. `npm run build` compiled successfully with `/app/queue` registered as a dynamic route. `git diff --check` clean.
- Database migration: `20260909043138_production_queue_planned_release`, generated and reviewed before application - additive only, a nullable `projects.planned_release_at` plus `projects_workspace_planned_release_index`, with no foreign key and so no repeat of the V2-04 composite `SET NULL` trap. Applied by executing the migration SQL directly rather than through `drizzle-kit push`, specifically so push could not rewrite the hand-written `scene_image_generations_tenant_batch_fkey` column list; the constraint was re-checked afterwards and is intact. No applied migration was edited.
- Note on migration ordering: this migration's folder timestamp sorts before `20260909053000_batch_delete_keeps_generations` although it was applied after it. The snapshot chain via `prevIds` is correct and the two migrations are independent, so no ordering hazard exists; the earlier folder name was chosen by hand and is not being renamed, since it is already applied and pushed.
- Backfill: none. No existing project was given a release date.
- Dependencies and environment variables added/changed: none.
- Deployment: not deployed. The migration is applied to the configured database; no Trigger.dev task changed, so no worker deploy is required.
- Rollback: revert the application changes and leave the additive column and index in place; existing queries ignore them.
- Known limitations: the queue has no browser verification. Audio blockers rely on succeeded generation counts rather than per-scene coverage, so a project with some narration reports no shortfall until a clip actually fails. `isComplete` treats any succeeded render as sufficient without checking it is the newest one.
- Measured production impact: not measured.

### V2-07 - Guided production workspace

- Owner: Claude. Started 2026-09-09, last updated 2026-09-10. Status: **In validation**. All slice scope is implemented and tested; no acceptance criterion has been observed in a browser.
- Scope delivered, first increment (2026-09-09): a project overview at the project root, a stage-appropriate primary action, stage progress derived from output, reusable-resource links, and an Overview tab. The root previously redirected to `/script`.
- Scope delivered, second increment (2026-09-10): the scene grid and scene detail unified into one **Scenes** context, and caption review moved into an **Assembly** context alongside preview and render. Nine tabs became seven.
- Central decision, first increment - **the overview shares the queue's derivation rather than repeating it**. `loadProjectProductionReadiness` calls the same page-facts loader V2-06 uses, so the overview and the queue cannot disagree about what a project needs or what to do next. No second readiness notion was introduced.
- Second decision - **every control on the overview is a link**. Opening a project must not start billable work or skip an approval, so the primary action navigates to where the decision is made and nothing more.
- Central decision, second increment - **group the tabs, do not merge the routes**. Scenes/Storyboard and Render/Subtitles were each one context shown two ways. Each pair is now one tab with a view switch, but both routes survive untouched. That is what makes "existing routes and bookmarks continue working" true by construction rather than by redirect, and it leaves the `revalidatePath` calls in the storyboard and subtitle server actions correct without change. It also avoids loading both views' data on every visit, which merging into one route would have forced.
- Fourth decision - **the shared state lives in the query string**. Which scene is open, the detail view's search and status filter, and the grid's image filter are held in the URL and mirrored with the history API rather than the router, so filtering costs no server round trip while the state still survives a reload and can be shared. State held in component memory would be discarded by the very navigation the criterion is about. Unrecognised values fall back to showing everything, because this is untrusted input from the address bar and a filter that cannot be honoured must not hide a creator's work.
- Fifth decision - **the view switch is a link, not a button**. That keeps bookmarking, opening in a new tab and the back button working, and it means the scene editor's unsaved-work guard, which watches link clicks, catches a change of view exactly as it catches leaving the page.
- Structural consequence: the detail view's shared state moved out of `SceneList`, which is now a controlled presentational component, into a new `SceneWorkspace` that owns selection, both filters and the unsaved-work guard for the whole context.
- Acceptance criteria addressed. _The representative workflow can advance without knowing all eight current tabs_ - met twice over: the overview names the next step and links to it, and the tab count is now seven, with a test asserting the register is shorter than the stage list it replaced. _Scene selection, filters, drafts, review state, and keyboard navigation survive view changes_ - implemented and unit-tested for selection and all three filters, in both directions; drafts are protected by the existing unsaved-work confirmation, which now also covers the view switch; keyboard navigation is preserved by using links and native controls but has **not been observed**, which is the main reason this slice is In validation rather than Verified. _Existing routes/bookmarks continue working or redirect to equivalent context_ - met; no route changed, and a test asserts every pre-existing segment still resolves to a tab that owns it. The production build lists all nine project routes. _Bulk generation and review remain available; failed scenes and per-operation costs remain visible_ - met; the storyboard toolbar, grid, failure actions and per-scene cost metadata are unchanged. _Navigation changes do not automatically trigger billable work or remove required editorial approval_ - met by construction; every added control is a link or a filter, and the caption summary in assembly is read-only.
- **Deliberate exception, recorded rather than hidden**: the grid's bulk checkbox selection does **not** survive a change of view. It stages a billable generation, so it is reconfirmed rather than restored from an earlier visit. The selected scene, which is what a creator is actually working on, does survive.
- Caption review wording is constrained by test: cues against an invalid timeline are never described as ready, and nothing states or implies that captions will appear in a render, because burning them in remains an explicit per-render choice.
- Evidence: [tab groups](../../lib/production/project-tab.ts) and [tests](../../lib/production/project-tab.test.ts), [view definitions](../../lib/production/workspace-views.ts) and [tests](../../lib/production/workspace-views.test.ts), [shared scene state](../../lib/production/scene-workspace-state.ts) and [tests](../../lib/production/scene-workspace-state.test.ts), [view switch](../../components/production/WorkspaceViewSwitch.tsx) and [tests](../../components/production/WorkspaceViewSwitch.test.ts), [scene workspace](../../components/scenes/SceneWorkspace.tsx), [grid deep link](../../components/storyboard/StoryboardSceneLink.tsx) and [tests](../../components/storyboard/StoryboardSceneLink.test.ts), [caption review wording](../../lib/render/caption-review-summary.ts) and [tests](../../lib/render/caption-review-summary.test.ts), [overview content](../../components/projects/ProjectOverviewPageContent.tsx).
- Commands executed and exact results, second increment: `npm test` passed **1603 tests across 264 files**, 128 skipped. `npm run typecheck` passed. `npm run lint` passed with zero errors and only the pre-existing `IDEA_PLATFORMS` warning. `npx prettier --check` on every changed and added file passed; the repository-wide check reports 39 pre-existing line-ending differences in files this slice did not touch. `npm run build` compiled successfully and lists all nine project routes. Database integration suites were run twice with `RUN_DATABASE_INTEGRATION_TESTS=true` and **could not be completed in this environment**: 36 of 126 tests failed, and the breakdown was 35 test or hook timeouts plus 18 connection errors with **zero AssertionError and zero expected/received diffs**. The cause was measured rather than assumed - five sequential `select 1` round trips to Neon, with no concurrency at all, returned 1432ms, a 10287ms connection failure, 771ms, a 10406ms connection failure, and 1399ms. Roughly two in five individual queries fail at about ten seconds each, so a test making twenty round trips is near-certain to time out. `--no-file-parallelism` did not help, which separates this from the known concurrent-connection problem. The same degraded link failed the first `git push` of this slice. **This slice changed no database, repository, command or query module**, so these suites exercise nothing it touched; they should be re-run when the link recovers.
- Database migration: none required, in either increment. No schema, dependency or environment change. No repository, command or query module was modified.
- Deployment: not deployed. No Trigger.dev task changed.
- Rollback: restoring the previous per-stage entries in `PROJECT_TABS` returns the nine-tab header; every route it points at still exists, so no other change is needed. Restoring the project root redirect reverses the first increment.
- Known limitations: no browser verification, so neither the overview nor the view switch has been observed rendering, and keyboard navigation across a view change is unproven. `npm run dev` and `npx playwright test` are refused by this environment's command permission policy. Subtitle export remains SubRip, which carries no position information, unchanged by this slice.
- Measured production impact: not measured.

### V2-XX - Title

- Status:
- Owner:
- Started / last updated / verified date:
- Scope delivered:
- Acceptance criteria met (reference plan criteria):
- Remaining criteria / next action:
- Architectural decisions and rationale:
- Changed files / commit or PR:
- Commands executed and exact results (pass/fail/skip):
- Browser/output review evidence (simulated or live):
- Database migrations: identifiers; generated/applied; target environment:
- Backfill: dry-run result, applied state, unresolved legacy rows:
- Dependencies and environment variables added/changed: none or exact names:
- Deployment: web/worker versions and environment; not deployed if applicable:
- Rollback or disable procedure:
- Known limitations / external finishing steps:
- Measured production impact:
```

## Decision log

| Date       | Scope            | Decision                                                                                                           | Status / consequence                                                   |
| ---------- | ---------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| 2026-09-08 | Entire plan      | Prioritize recurring YouTube production and preserve current architecture                                          | Planning baseline; no implementation authorized by this document alone |
| 2026-09-08 | Marketing Studio | No Marketing Studio feature improvements; preserve shared-service compatibility                                    | Video analytics independence remains in scope                          |
| 2026-09-08 | Delivery         | Start with baseline and revision safety; require evidence before completion                                        | All implementation slices Not started                                  |
| 2026-09-08 | V2-00            | Creator selected Money Made Clear; weekly 8-12 minute faceless finance with 3-5 clips and mixed/programmatic media | Resolved; measured walkthrough remains pending                         |
| Pending    | V2-12            | Alignment implementation and paid-provider budget                                                                  | Evaluate fixtures before provider selection                            |
| Pending    | V2-09/10/17      | Account scopes, supported release/analytics capabilities                                                           | Verify official docs and actual account access during implementation   |

## Money Made Clear extension

- Creator decision: [confirmed channel brief](money-made-clear.md); faceless weekly 8-12 minute long-form, 3-5 distinct vertical clips, mixed footage/stills, programmatic financial graphics, low-volume music/selective effects. Animated characters are not core scope.
- Technical delivery: test-only channel profile/visual manifest, a roughly ten-minute 40-scene fixture, and four 45-second candidates. The existing generic fixture remains unchanged by default.
- Required roadmap scope added: V2-13 programmatic charts/tables/callouts/screen examples and inspected video; V2-14 music and selective effects; V2-15 3-5 distinct clips and vertical graphic reflow for existing platform destinations.
- Verification: `npx vitest run lib/test-utils db/commands/scene-revision-baseline.test.ts lib/timeline lib/shorts lib/subtitles lib/render lib/publishing/providers/simulated-video-publish-provider.test.ts lib/domain/bulk-scene-image.test.ts` passed 146 tests across 26 files (five additional Money Made Clear tests). `npm run typecheck` passed; `npm run lint` completed with zero errors and the existing `IDEA_PLATFORMS` warning. Prettier check, 43 local documentation links, and `git diff --check` passed. No database, provider, generated media, migration, environment, dependency, or deployment change. Database integration, live production walkthrough, and rendering were not run; production build is not applicable to these test/documentation-only changes.
- Limitation: the production renderer still executes still/audio proxies; this manifest is not shipped mixed-media support. V2-00 remains In progress pending a measured walkthrough.

## Change log

- 2026-09-10: Corrected a database integration test that had been red since 10 August. `approveSceneAudioGeneration` gained a guard refusing to approve a browser recording until its media inspection has succeeded, because until then nothing has confirmed the uploaded bytes are playable audio of the stated length; the recorded-narration test predated that guard by two weeks and still approved a recording directly. It now asserts the rule in both directions, and that file passes 10 of 10. No production code changed. Confirmed pre-existing by reproducing the identical failure on the commit before this slice. Separately, the database integration suites could not be completed in this environment: every remaining failure is a timeout or a connection error with no assertion failure, and measured Neon round trips show roughly two in five queries failing at about ten seconds each.
- 2026-09-10: Completed V2-07. Scenes and Storyboard became two views of one Scenes tab, and Render and Subtitles two views of one Assembly tab, taking the project from nine tabs to seven without changing a single route, so links and bookmarks are unaffected. The scene a creator is on and their three filters travel across a change of view in the query string, so they also survive a reload and can be shared; the grid rings the scene open in detail and links back to it. The grid's bulk selection is deliberately not carried across, because it stages billable work. Assembly gained a read-only caption review summary linking into the captions view, whose wording is test-constrained never to call cues ready against an invalid timeline nor to imply captions will appear in a render. Slice moves to In validation: everything is implemented and tested, but no criterion has been observed in a browser.
- 2026-09-09: Started V2-07 with its navigation increment. The project root is now an overview with a stage-appropriate next step instead of a redirect to the script tab, reusing the V2-06 readiness resolver so the overview and the queue cannot disagree. Every control on it is a link, so opening a project cannot start billable work. Tab resolution became a pure function and a test asserts every pre-existing route still lands on the tab it always had. The scene grid/detail unification and caption-in-assembly parts of the slice are not delivered, so V2-07 stays In progress.

- 2026-09-09: Delivered V2-06, the production queue and derived readiness. Readiness is computed from scripts, scenes, assets, renders and publications, and is structurally unable to read the hand-editable project status, so a project labelled "completed" with nothing produced still reports that no script has been written. Published, rendered and scheduled are distinct, and a planned release date never outranks a real render. Fixed a real spend defect: the dashboard presented image-generation cost alone as overall spend, and now reuses the usage read model. Two further defects were caught by live-database tests that unit tests could not see, an inverted `ORDER BY ... nulls last` and an empty seeded script draft being counted as written work.

- 2026-09-09: Delivered V2-05 reusable formats and the channel idea backlog. Format presets are versioned and immutable, projects snapshot the exact version they inherited, and a live test proves editing a format leaves an in-progress project on its original definition. Saved-idea reuse is now visible history rather than a silent block. Applied the V2-04 foreign-key lesson: no composite SET NULL anywhere, so deleting a cited format version is refused while workspace cleanup still cascades.

- 2026-09-09: Second V2-03 increment. Extracted the validate-and-repair policy from the Trigger.dev task into an injectable unit so it could be tested against a fake provider, closing a gap between the first increment's stated plan and what it delivered; added nine mocked-provider tests covering repair, bounds and usage accumulation, plus an authenticated Playwright scenario that remains unexecuted because the development server is owned by the creator and was not started. A read-only Trigger.dev check confirmed prod worker 20260908.2 predates this slice, so production still runs the unvalidated scene-analysis task and a worker deploy is required before the gate has any effect on real runs.

- 2026-09-09: Delivered V2-03 verified script-to-scene fidelity. Scene analysis now rejects a plan whose concatenated narration does not cover the approved script exactly once and in order, under a narrow documented normalization policy; paid output that fails validation records its provider usage and reconciles its reservation instead of being released at zero; the previous scene plan survives a rejected run; and the scenes page compares approved-script narration against the current plan. No migration was required and no environment variable or dependency changed. Also reconciled this tracker with git history: the V2-01 third increment and V2-02 records previously said they were not pushed, but both are contained in commit 20f8984 on origin/master. They remain undeployed.

- 2026-09-08: Started V2-00 with executable synthetic production fixtures and current scene-edit characterization. Seven new tests passed; live creator measurements and channel choices remain pending. No product behavior or database state changed.

- 2026-09-08: Created version-two plan, slice register, and validation protocol; linked from root README. Documentation only. Audit-era test results are historical context, not completion evidence for any new slice.
